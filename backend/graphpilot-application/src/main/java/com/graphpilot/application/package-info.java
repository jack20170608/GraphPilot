/**
 * Framework-free GraphPilot application layer.
 *
 * <p>This module defines use cases, commands, queries, inbound ports, outbound ports,
 * and application services. It depends only on the domain module.</p>
 */
package com.graphpilot.application;
